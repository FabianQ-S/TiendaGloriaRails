<% if category.errors.any? %>
  <div class="form-errors">
    <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: [:admin, category], local: true do |f| %>
  <div class="form-group">
    <label class="form-label">🏷️ Nombre</label>
    <%= f.text_field :name, class: "form-control", required: true %>
  </div>
  
  <div class="form-group">
    <label class="form-label">📂 Categoría Padre (opcional)</label>
    <%= f.collection_select :parent_id, Category.where.not(id: category.id).order(:name), :id, :name, { include_blank: "Sin categoría padre" }, { class: "form-control" } %>
  </div>
  
  <div class="form-actions">
    <a href="<%= admin_categories_path %>" class="btn btn-secondary">Cancelar</a>
    <%= f.submit "Guardar", class: "btn btn-success" %>
  </div>
<% end %>
