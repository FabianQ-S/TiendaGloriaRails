<div class="form-container">
  <h2 class="form-title">🔑 Iniciar Sesión</h2>
  
  <%= form_with url: login_path, method: :post, local: true do |f| %>
    <div class="form-group">
      <label class="form-label">📧 Email</label>
      <%= email_field_tag :email, params[:email], class: "form-control", placeholder: "tu@email.com", required: true %>
    </div>
    
    <div class="form-group">
      <label class="form-label">🔒 Contraseña</label>
      <%= password_field_tag :password, nil, class: "form-control", placeholder: "Tu contraseña", required: true %>
    </div>
    
    <div class="form-actions">
      <%= submit_tag "Ingresar", class: "btn btn-primary btn-lg" %>
    </div>
  <% end %>
  
  <p class="text-center mt-3">
    ¿No tienes cuenta? <a href="<%= register_path %>">Regístrate aquí</a>
  </p>
</div>
