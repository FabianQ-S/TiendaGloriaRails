# Fixtures para Batches (Lotes)

lote_vigente:
  batch_number: "LOTE-TEST-001"
  expiration_date: <%= Date.current + 30.days %>
  quantity: 100

lote_vencido:
  batch_number: "LOTE-TEST-002"
  expiration_date: <%= Date.current - 10.days %>
  quantity: 50

lote_por_vencer:
  batch_number: "LOTE-TEST-003"
  expiration_date: <%= Date.current + 5.days %>
  quantity: 25
